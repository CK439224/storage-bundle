<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing this app stores may leave the device (PLAN.md 6). The OCR index in particular
  can contain text lifted off the user's screen, so cloud backup and device transfer are
  both fully excluded rather than filtered.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
